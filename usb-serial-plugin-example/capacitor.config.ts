import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'usb-serial-plugin-example',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
